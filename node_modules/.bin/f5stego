#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../f5stegojs/bin/f5stego" "$@"
  ret=$?
else 
  node  "$basedir/../f5stegojs/bin/f5stego" "$@"
  ret=$?
fi
exit $ret
